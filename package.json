{
  "name": "auto-standup-bot",
  "version": "0.1.0",
  "description": "Auto-generate daily standup summaries from Jira activity and post them to Slack",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "start:tunnel": "forge tunnel",
    "deploy": "forge deploy",
    "deploy:prod": "forge deploy -e production",
    "logs": "forge logs --tail"
  },
  "dependencies": {
    "@forge/api": "^4.0.0",
    "@forge/resolver": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "license": "UNLICENSED",
  "private": true
}
